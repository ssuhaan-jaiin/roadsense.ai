// Stub for Data.gov API interactions
export async function fetchDataGov(endpoint, key = import.meta.env.VITE_DATAGOV_KEY) {
  // implement request to data.gov using endpoint and key
  return { data: null }
}
